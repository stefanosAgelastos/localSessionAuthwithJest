import mongoose from "mongoose";
import serverConfig from "../server.config";

export default async () => {
  const connection = await mongoose.connect(serverConfig.mongoURL, {
    useNewUrlParser: true,
    useCreateIndex: true
  });
  console.log("connected db");
  return connection;
};